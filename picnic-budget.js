const numberOfMamber = picnicBudget(1123);
console.log(numberOfMamber);

const friends = ['abul', 'habul', 'cabul', 'hasem', 'mohin']
function oddFriend(firned) {
    for (let i = 0; i < friends.length; i++) {
        if (friend[1].length % 2 != 0) {
            returnfriend[1];
        }
    }
}
const oddFriendName = oddFriend(friends);
console.log(oddFriendName);